import React from "react"

type Props = {}

const Footer = (props: Props) => {
  return (
    <div>
      This area will have information about plants, tips on care and different
      ways to add humidity.
    </div>
  )
}

export default Footer
